### Follow these instructions to run tests using Docker and Docker Compose.

- **Build Image**: `npm run build`
- **Run Docker Compose**: `npm run docker`
- **Run Wdio Contianer**: `npm run container`

